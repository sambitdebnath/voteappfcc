<!DOCTYPE html>
<html>
    <head>
        <title>Gimme some votes</title>
    </head>
    <body>
        <h1>Vote Machine v1.3</h1>
        <form action="/" method="post">
            <% if (message.length > 0) { %>
                <div><%= message %></div>
            <% } %>
            <div>
                <label>Email</label>
                <input type="text" name="email">
            </div>
            <div>
                <label>Password</label>
                <input type="password" name="password">
            </div>
            <button type="submit">Log in</button>
        </form>
        <p><a href="/signup">Sign up</a> and start feeding you opinions to people</p>
    </body>
</html>